#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<long double>arr(n);
    for(auto&x:arr)cin>>x;
    ranges::sort(arr);
    for(int i=1;i<n;i++){
        arr[i]=(arr[i]+arr[i-1])/2;
    }
    cout<<fixed<<setprecision(9);
    cout<<arr.back();
}
#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    long long n,m;
    cin>>n>>m;
    n*=m,m=0;
    vector<int>arr(n);
    for(auto&x:arr)cin>>x,m+=x;
    cout<<m-ranges::min(arr);
}
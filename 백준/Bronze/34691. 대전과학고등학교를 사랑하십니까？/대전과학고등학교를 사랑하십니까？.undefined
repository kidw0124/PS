#include<bits/stdc++.h>
using namespace std;
int main(){
    string str;
    while(cin>>str){
        if(str=="animal")cout<<"Panthera tigris\n";
        if(str=="flower")cout<<"Forsythia koreana\n";
        if(str=="tree")cout<<"Pinus densiflora\n";
    }
}
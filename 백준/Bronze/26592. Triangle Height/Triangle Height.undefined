#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    while(n--){
        double a,b;
        cin>>a>>b;
        cout<<fixed<<setprecision(2);
        cout<<"The height of the triangle is "<<2*a/b<<" units\n";
    }
    
}
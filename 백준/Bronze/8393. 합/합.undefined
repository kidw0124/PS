#include<bits/stdc++.h>
using namespace std;
using ll=long long;
int main(){
    ll n; cin>>n;
    vector<ll>arr(n);
    iota(arr.begin(),arr.end(),1);
    cout<<accumulate(arr.begin(), arr.end(), 0);
}
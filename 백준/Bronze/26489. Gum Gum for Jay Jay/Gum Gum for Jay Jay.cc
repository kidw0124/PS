#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    string s;
    int i=0;
    while(getline(cin,s)&&s.size()>0){
        ++i;
    }
    cout<<i;
}